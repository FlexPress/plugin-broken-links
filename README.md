# FlexPress broken links plugin

## Install
Install via composer

```
composer require flexpress/plugin-broken-links 1.0.*
```

Then you will want to to go WPAdmin > Broken links > Run link checker to run the link checker.